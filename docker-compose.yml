version: '3.1'

services:
  api:
    image: master-image
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src/:/usr/src/app
      - ../data/dogpile_data:/opt/project/dogpile_data
    ports:
      - "5000:5000"
    env_file: ./master_image.env
    command: flask run --host=0.0.0.0
    depends_on:
      - database
      - redis
      - nginx
      - rq_dashboard

  #  client:
  #    build:
  #      context: .
  #      dockerfile: ../smp-client/Dockerfile
  #    volumes:
  #      - ../smp-client:/app
  #    ports:
  #      - "3000:3000"
  #    command: yarn start

  rq_worker:
    image: master-image
    command: rqworker -n extra_worker_001 -u redis://redis:6379/0 -v
    env_file: ./master_image.env
    depends_on:
      - smtp_server
      - redis
    environment:
      - SENTRY_DSN=https://6748ed500c474e2899c1bd7ba0d0b755@o932546.ingest.sentry.io/5883757

  rq_dashboard:
    image: master-image
    ports:
      - 5555:5555
    depends_on:
      - redis
      - rq_worker
    command: /usr/local/bin/rq-dashboard --port 5555 --redis-url redis://redis:6379/0

  test_app:
    image: master-image
    env_file: ./master_image.env
    depends_on:
      - smtp_server
      - redis

  database:
    image: postgres:12
    environment:
      POSTGRES_PASSWORD: clone
      POSTGRES_DB: smp_db
    ports:
      - "5432:5432"
    volumes:
      - ../data/postgres-data:/var/lib/postgresql/data

  #  sentry_db:
  #    image: postgres
  #    environment:
  #      POSTGRES_USER: sentry
  #      POSTGRES_PASSWORD: sentry
  #      POSTGRES_DB: sentry
  #    volumes:
  #     - ../data/postgres-sentry-data:/var/lib/postgresql/data

  smtp_server:
    image: mailhog/mailhog
    ports:
      - "8025:8025"
      - "1025:1025"
    environment:
      - MH_SMTP_BIND_ADDR=0.0.0.0:2525

  redis:
    image: redis:5
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf
      - ../data/redis:/data
    ports:
      - "6379:6379"

  minio:
    image: minio/minio
    restart: unless-stopped
    hostname: minio
    volumes:
      - ../data/minio:/data
    expose:
      - "9000"
      - "9001"
    environment:
      MINIO_ROOT_USER: Q3AM3UQ867
      MINIO_ROOT_PASSWORD: Vh5smyLeyCZd^$$4m9pmb7T16!DwyU18kvPJFiCSG
    command: server /data --console-address ":9001"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9000/minio/health/live" ]
      interval: 30s
      timeout: 20s
      retries: 3

  #  sentry:
  #    image: sentry
  #    links:
  #      - redis
  #      - sentry_db
  #    ports:
  #      - "9000"
  #    environment:
  #      SENTRY_SECRET_KEY: 'ee779b0ceede11ebb0fa1645c821b837'
  #      SENTRY_POSTGRES_HOST: database
  #      SENTRY_DB_USER: sentry
  #      SENTRY_DB_PASSWORD: sentry
  #      SENTRY_REDIS_HOST: redis

  nginx:
    image: nginx
    hostname: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "9000:9000"
      - "9001:9001"
    depends_on:
      - minio
#      - sentry